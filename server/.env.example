# Copy to .env and adjust. Used by docker-compose.
# Shared secret the device must send in X-Device-Token. Empty = open endpoint.
INGEST_TOKEN=
# Store at most one full spectrum per device per this many seconds.
SPECTRUM_MIN_INTERVAL_S=30
# Delete readings/spectra older than N days. 0 = keep forever.
RETENTION_DAYS=0
